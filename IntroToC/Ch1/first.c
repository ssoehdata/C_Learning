#include <stdio.h>   // '#'  include<stdio.h> is a directive (this one loads the stdio header file)
// int - return type
// main - name of function (entry point)
// (void) - the function takes no arguments 

int main(void) {    
    printf("First program.\n");
    return 0;
}
